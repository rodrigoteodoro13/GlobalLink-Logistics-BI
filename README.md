This is a simulated logistics company and this report shows an overview on the main KPI's of a logistics company with 4 branches. 

I also created a hidden page to be used as tooptip when hover the branches bar chart with the top 5 drivers of each branch. For this I had to resolve an ambiguous path by creating a new DAX measure with USERELATIONSHIP function, because I could not resolve through the model relation.
